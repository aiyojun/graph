// Target: render whole web page by route
// Then, push forward and go back functions
const renderWebpageByRoute = (url: string, renderTree: any) => {}
const stack: Array<string> = []
const navigateTo = () => {}
const backStep = () => {}